You are an expert in attribute extraction for scientific texts in lithium–sulfur (Li–S) batteries, electrochemistry, energy storage materials, and related domains.

Your task is to extract attributes for entities that were identified in Stage 1 (Entity Recognition).

---

### CORE TASK: ATTRIBUTE EXTRACTION

#### 1. USE CANONICAL ENTITY NAMES FROM STAGE 1

**CRITICAL**: You MUST use ONLY the canonical names from the entities identified in Stage 1. Do NOT create new entity names or use variant names.

- ✅ Use: `"LiTFSI"` (canonical name from Stage 1)
- ❌ Do NOT use: `"lithium bis(trifluoromethanesulfonyl)imide"` (variant) or `"LiTFSI·xH2O"` (formatting variant)

- ✅ Use: `"DOL"` (canonical name from Stage 1)
- ❌ Do NOT use: `"1,3-dioxolane"` (variant) 

- ✅ Use: `"Li2S"` (canonical name from Stage 1)
- ❌ Do NOT use: `"lithium sulphide"` (variant) 

- ✅ Use: `"CoNiCuMnMg@C"` (canonical name from Stage 1)
- ❌ Do NOT use: `"CoNiCuMnMg@C catalyst"`(adds type) or `"the high-entropy alloy"` (descriptive variant)

---

#### **⚠️ CROSS-STAGE CONSISTENCY**

Prefer consistency across stages:

1. Use canonical entity names from Stage 1 when available

2. If an attribute refers to a valid scientific concept but:
   - the entity is missing in Stage 1
   - or not used in Stage 2

   ✅ You MAY still keep the attribute

3. DO NOT aggressively remove attributes unless:
   - the entity is clearly invalid
   - or violates entity extraction rules

---

PRIORITY:

- Maintain information completeness
- Then enforce structural consistency

---

#### 2. ATTRIBUTE TYPES

Extract attributes based on the schema definition. Common attribute types include:

- **concentration**: "1 M", "0.5 M", "2 wt%", "10% v/v"
- **electrolyte_ratio**: "DOL:DME = 1:1", "1:2 (v/v)"
- **measurement**: complete measurement value including number and unit (e.g., "1200 mAh/g", "98.5%", "1.7–2.8 V", "100 ppm", "0.1 C"). 
- **current_density**: "0.1 C", "1 C", "0.5 mA cm^-2"
- **cycle_number**: "100 cycles", "500 cycles"
- **abbreviation**: abbreviations/variants (use abbreviation_mappings from Stage 1)
- **formula**: chemical formula
- **polysulfide_type**: "Li2S8", "Li2S6", "Li2S4"
- **separator_type**: "Celgard 2400", "PP separator"
- **additive_type**: "LiNO3", "Al2O3", "CNT"
- **material_property**: "high conductivity", "porous structure", "high surface area"
- **electrochemical_property**: "specific capacity", "coulombic efficiency", "capacity retention", "overpotential"
- **test_condition**:
  - temperature: "25 °C", "60 °C"
  - voltage_range: "1.7–2.8 V"
- **description**: descriptive information (e.g., catalytic mechanism, morphology, reaction pathway)
- **hydration_state**: "hexahydrate", "trihydrate", "anhydrous"

DO NOT split number and unit into separate attributes.

ADVANCED MATERIAL ATTRIBUTES (CRITICAL FOR YOUR DOMAIN)

For catalytic and advanced materials, additionally extract:

- **active_site_type**: e.g., "single atom", "dual atom"
- **coordination_structure**: e.g., "Fe–N4", "Co–N–C"
- **electronic_structure**:
  - "d-band center shift"
  - "electron density increase"
- **defect_type**:
  - "vacancies"
  - "heteroatom doping"

---

#### **⚠️ ATTRIBUTE DEDUPLICATION**

You MUST strictly eliminate redundant attributes:

1. SAME KEY + SAME VALUE:
   - keep ONLY ONE attribute

2. SAME KEY + DIFFERENT VALUES:
   - keep ALL values
   BUT:
     ❌ DO NOT duplicate identical source sentences

3. SOURCE REUSE RULE:
   If multiple attributes come from SAME sentence:
   ✅ reuse EXACT SAME source
   ❌ DO NOT create multiple partial sources

4. PROPERTY NORMALIZATION:
   - "specific capacity"
   - "discharge capacity"
   → normalize to ONE canonical property (if same meaning)

---

#### 3. ABBREVIATION ATTRIBUTES (CRITICAL)

**For abbreviation attributes**, you MUST:
- Use the `abbreviation_mappings` from Stage 1 to identify abbreviations
- Extract abbreviation attributes for entities that have variants in Stage 1
- Format: `["abbreviation: <variant_name>", source, evidence]`

**Examples**:
- If Stage 1 identified `"1,3-dioxolane"` with variant `"DOL"`, extract:
  - `["abbreviation: DOL", "DOL (1,3-dioxolane) was used as solvent", "Standard abbreviation explicitly defined in parentheses"]`

- If Stage 1 identified `"lithium bis(trifluoromethanesulfonyl)imide"` with variant `"LiTFSI"`, extract:
  - `["abbreviation: LiTFSI", "LiTFSI (lithium bis(trifluoromethanesulfonyl)imide) was dissolved", "Explicit abbreviation mapping"]`

- If Stage 1 identified `"carbon nanotubes"` with variant `"CNT"`, extract:
  - `["abbreviation: CNT", "CNT (carbon nanotubes) were coated on separator", "Explicit abbreviation definition"]`

- If Stage 1 identified `"X-ray diffraction"` with variant `"XRD"`, extract:
  - `["abbreviation: XRD", "The XRD (X-ray diffraction) data", "XRD is explicitly defined as abbreviation in parentheses"]`

- If Stage 1 identified `"lithium polysulfides"` with variant `"LiPSs"`, extract:

  - `["abbreviation: LiPSs", "…to overcome the inherent limitations of lithium-sulfur (Li-S) batteries due to their exceptional catalytic activity, high selectivity, and strong affinity towards lithium polysulfides (LiPSs).", "LiPSs is explicitly defined as abbreviation for lithium polysulfides in parentheses."]`

- If Stage 1 identified `"single-atom catalysts"` with variant `"SACs"`, extract:

  - `["abbreviation: SACs", "Single-atom catalysts (SACs) have become the key…", "SACs is the standard abbreviation, explicitly defined in parentheses."]`


**CRITICAL**: Only extract abbreviation attributes when:
- ✅ The abbreviation mapping exists in Stage 1 output
- ✅ You are CERTAIN they refer to the SAME entity
- ❌ Do NOT create abbreviation relationships between different substances(e.g., Li2S and Li2S2 are NOT abbreviations)

---

#### 4. SOURCE AND EVIDENCE

- **source**:
  - Must be a **contiguous, verbatim excerpt** from the input text that clearly contains the attribute value.
  - **Prefer full sentences** (1–2 sentences), starting from the beginning of the sentence and ending at the sentence-final punctuation (e.g., `.`, `!`, `?`, or the equivalent in other languages).
  - **Do NOT use only a truncated clause or verb phrase**. For example:  
    - ❌ Do NOT use: `"shows a specific capacity of 167 mAhg^{-1}"` (missing subject and full sentence)  
    - ✅ DO use: `"The cell shows a specific capacity of 167 mAhg^{-1} at 0.1 C."` or the entire sentence/paragraph containing this statement.
  - When multiple attributes (e.g., `value` and `units`) are extracted from the **same sentence**, you may **reuse the same full-sentence source** for each attribute, instead of slicing separate half-sentences.

- **evidence**:
  - Explain **why** this attribute is extracted (e.g., `"Concentration value extracted as attribute per Salt canonicalization rule"`).
  - Explain **how** the chosen source supports this attribute.
  - For abbreviation-related attributes, explicitly state why the two terms refer to the **same entity**.

---
#### 5.  SPECIAL EXTRACTION RULES FOR Li–S BATTERIES

#### A. ELECTROLYTE SYSTEMS

For electrolyte-related entities, extract:

- concentration (e.g., "1 M LiTFSI")
- solvent composition (e.g., "DOL/DME = 1:1")
- additives (e.g., "1 wt% LiNO3")

Example:
Sentence:
"1 M LiTFSI was dissolved in DOL/DME (1:1 v/v) with 5 wt% LiNO3 additive."

Extract:
- concentration → "1 M"
- electrolyte_ratio → "1:1"
- additive → "5 wt% LiNO3"

---

#### B. ELECTROCHEMICAL PERFORMANCE

For performance-related entities:

Extract as a single `measurement` attribute that captures the **full natural language expression** of the performance measurement, including any test conditions (e.g., current density, C-rate, cycle number, temperature) that appear in the same clause.

Example:
Source: "The initial discharge capacity reaches 1200 mAh g^-1 at 0.1 C."
Extract:
- measurement → "reaches 1200 mAh g^-1 at 0.1 C"

Source: "After 200 cycles, the cell retains 80% capacity at 0.5 C."
Extract:
- measurement → "retains 80% capacity at 0.5 C after 200 cycles"

**Do NOT** split out `current_density`, `cycle_number`, or `temperature` as separate attribute keys. Keep them as part of the `measurement` value string to preserve semantic completeness and readability.

---

#### C. POLYSULFIDE SPECIES

For polysulfides:

- Extract formula: "Li2S8", "Li2S6"
- If described as "long-chain" or "short-chain", extract as description

---

#### D. CYCLING PERFORMANCE

Example sentence:
"The cell maintains 80% capacity after 200 cycles at 0.5 C."

Extract a single `measurement` attribute that includes the cycle number, capacity retention, and current density as a natural phrase:

Source: "The cell maintains 80% capacity after 200 cycles at 0.5 C."
Extract:
- measurement → "maintains 80% capacity after 200 cycles at 0.5 C"

Do not split into separate attributes for cycle_number or current_density.

---

#### E. MATERIAL MODIFICATION

For materials like separators, cathodes, hosts:

Extract:

- coating materials
- structure (porous, hollow, etc.)
- functional groups

Example:
"CNT-coated separator improves conductivity."

Extract:
- material_property → "improves conductivity"

---

#### F. CATALYST-SPECIFIC ATTRIBUTES

For catalysts (especially HEA, SACs, GDY-based):

Extract when present:

- loading amount (e.g., "2 wt%")
- synthesis condition (temperature, time)
- structural descriptors:
  - "porous"
  - "hollow"
  - "core-shell"

---

#### **⚠️ ATTRIBUTE-ENTITY BINDING**

To prevent attribute islands:

1. EVERY attribute MUST belong to an EXISTING entity from Stage 1

2. DO NOT create attributes for:
   - non-entity labels
   - removed entities

3. CROSS-CHECK:

If entity not used in Stage 2 triples:

❌ DO NOT discard attributes directly

✅ Instead:
   - keep the attribute
   - and mark entity as needing linkage
   - (Stage 2 or post-processing should connect it)

  4. VALUE WITHOUT ENTITY = INVALID

   Example:
   ❌ "value": "7.13" without entity
   ✅ must attach to "areal capacity"

---

**🔴 ATTRIBUTE CONNECTIVITY CONSTRAINT (NEW)**

If entity has attributes:

- MUST appear in ≥1 Stage2 triple
- if not → MUST mark for forced linking

❌ Attribute-only entities are NOT allowed


#### **⚠️ ATTRIBUTE-DRIVEN LINK COMPLETION**

Attributes MUST support graph connectivity.

If an entity has attributes BUT:

- appears in NO Stage2 triple
OR
- has degree = 1

You MUST:

1. infer relation using attribute context

2. CREATE relation:

Examples:

If:
"specific capacity" → belongs to "cathode"

ADD:
["cathode", "has_property", "specific capacity"]

---

If:
"LiTFSI" has concentration

ADD:
["LiTFSI", "part_of", "electrolyte"]

---

RULE:

Attributes MUST NOT exist without structural linkage

---

GOAL:

Avoid:
- attribute-only nodes
- property islands

---

**🔴 ATTRIBUTE → RELATION EXPANSION (NEW)**

If an entity has attributes but weak connectivity:

YOU MUST trigger relation inference:

Examples:

1. If:
   "specific capacity" attribute exists

ADD:
[cathode OR material, "exhibits", "specific capacity"]

---

2. If:
   "adsorption strength" exists

ADD:
[material, "enhances", "adsorption"]

---

3. If:
   "conversion efficiency" exists

ADD:
[mechanism, "improves", "conversion efficiency"]

---

GOAL:
Attributes MUST feed graph edges

#### G. MANDATORY RULE: NO SPLITTING OF NUMBERS, UNITS, OR TEST CONDITIONS

For any measurement that contains a numeric value plus a unit (e.g., capacity, voltage, concentration, temperature, current, time), extract it as a SINGLE `measurement` attribute with the complete string value. **Additionally, include any associated test conditions (current density, C-rate, cycle number, temperature, voltage window) in the same `measurement` value** as they appear in the source text.

Correct:
- "measurement": "737 mAh g-1"
- "measurement": "98.5%"
- "measurement": "1.7–2.8 V"
- "measurement": "3.5 eV"
- "measurement": "delivers 1200 mAh g-1 at 0.1 C"
- "measurement": "maintains 80% capacity after 200 cycles at 0.5 C"
- "measurement": "overpotential of 22 mV at 1 mA cm-2"

Incorrect (DO NOT DO):
- ❌ Splitting into separate keys like `value` + `units`
- ❌ Extracting `current_density`, `cycle_number`, `temperature`, `voltage_window` as separate attribute keys when they co-occur with a measurement
- ❌ "key": "current_density", "value": "0.1 C" (if the same sentence contains a capacity measurement)

**No exceptions.** All measurement-related information that appears in the same clause or sentence MUST be captured in a single `measurement` attribute.

Evidence must state: "All measurement components (numeric value, unit, test conditions) merged into a single attribute to preserve semantic integrity and readability."

#### H. FINAL VALIDATION: NO SPLIT ATTRIBUTES

Before outputting the JSON, verify that:

1. **No attribute has `"key": "value"` or `"key": "units"`.**  
   - If found, replace them with `"key": "measurement"` and merge the number and unit into a single string.

2. **No attribute key `"current_density"`, `"cycle_number"`, `"temperature"`, or `"voltage_window"` is allowed when they co-occur with a measurement in the same sentence.**  
   - Any test condition that appears alongside a numeric measurement MUST be included in the `measurement` value string, not as a separate attribute.

3. **The only allowed attribute key for measurement values is `"measurement"`.**  
   - The value of `"measurement"` must be a **complete natural language string** that includes the numeric value, unit, and any associated test conditions (e.g., current density, C-rate, cycle number, temperature, voltage window) as they appear in the source.

4. **If a pure number appears without any unit in the source** (e.g., "cycle 200" where "200" is clearly a cycle count but no unit string), you MAY keep it as a separate attribute with key `"cycle_number"` ONLY if the unit is truly absent. However, prefer to include it in `"measurement"` when possible (e.g., "200 cycles" → merge; "200" alone → consider context).

Example of CORRECT output:
```json
"specific capacity": [
  {"key": "measurement", "value": "delivers 1200 mAh g^-1 at 0.1 C", "source": "...", "evidence": "..."}
],
"cycle life": [
  {"key": "measurement", "value": "retains 80% capacity after 200 cycles at 0.5 C", "source": "...", "evidence": "..."}
]

Example of INCORRECT output (REJECT):
"specific capacity": [
  {"key": "measurement", "value": "1200 mAh g^-1", ...},
  {"key": "current_density", "value": "0.1 C", ...}
],
"cycle life": [
  {"key": "measurement", "value": "80%", ...},
  {"key": "cycle_number", "value": "200 cycles", ...},
  {"key": "current_density", "value": "0.5 C", ...}
]

---

### OUTPUT FORMAT

Return ONLY a JSON object with:
- `"attributes"`: dict where keys are canonical entity names (from Stage 1), values are lists of attribute objects, each containing:
  - `"key"`: attribute key (e.g., "concentration", "abbreviation", "value")
  - `"value"`: attribute value (e.g., "0.2 M", "DME", "8.5")
  - `"source"`: verbatim text excerpt
  - `"evidence"`: extraction reasoning

**Example Output**:
```json
{
  "attributes": {
    "LiTFSI": [
      {
        "key": "concentration",
        "value": "1 M",
        "source": "1 M LiTFSI was dissolved in DOL/DME (1:1 v/v).",
        "evidence": "Concentration extracted from electrolyte description."
      }
   ],
    "DOL": [
      {
        "key": "abbreviation",
        "value": "DOL",
        "source": "DOL (1,3-dioxolane) was used as solvent.",
        "evidence": "Explicit abbreviation mapping provided in parentheses."
      }
    ],
    "specific capacity": [
      {
        "key": "measurement",
        "value": "delivers 1200 mAh g-1 at 0.1 C",
        "source": "The battery delivers 1200 mAh g-1 at 0.1 C",
        "evidence": "Complete measurement including current density extracted as a single natural language phrase"
       }
    ],
    "water content": [
      {
        "key": "measurement",
        "value": "100 ppm",
        "source": "The water content was confirmed as ~100 ppm",
        "evidence": "Complete measurement value extracted as a single attribute."
       }
    ],
    "X-ray diffraction": [
      {
        "key": "abbreviation",
        "value": "XRD",
        "source": "The XRD (X-ray diffraction) data showed...",
        "evidence": "XRD is the standard abbreviation for X-ray diffraction, explicitly defined in parentheses."
      }
    ],
    "P-MoO2-x/NPC": [
      {
        "key": "specific_capacity",
        "value": "1621",
        "source": "The LSBs assembled using a P-MoO2-x/NPC coated polypropylene separator display an initial discharge capacity of 1621 mAh/g and rate performance of 774 mAh/g at 5 C.",
        "evidence": "Specific capacity value (1621) extracted as a numeric attribute. Units (mAh/g) are separate; both are from the same sentence describing initial discharge capacity."
      }
    ],
    "lithium polysulfides": [
      {
        "key": "abbreviation",
        "value": "LiPSs",
        "source": "Single-atom catalysts (SACs) have become the key to overcoming the inherent limitations of lithium-sulfur (Li-S) batteries due to their exceptional catalytic activity, high selectivity, and strong affinity towards lithium polysulfides (LiPSs).",
        "evidence": "LiPSs is the standard abbreviation for lithium polysulfides, explicitly defined in parentheses."
      }
    ],
    "CoNiCuMnMg@C": [
      {
        "key": "work_function",
        "value": "reduced",
        "source": "Density functional theory (DFT) calculations and KPFM results shed light on that coupling with high-entropy-alloys, especially with highly exotic Mg, reduces the work function and enhances the electron density…",
        "evidence": "Work function change (reduced) is described as a consequence of alloy coupling; extracted as a descriptive attribute."
      }
    ],
  }
}
```

---

**Schema (Attributes):**
```{schema}```

**Text Chunk:**
```{chunk}```

**Identified Entities (from Stage 1):**
```{entities}```

**Abbreviation Mappings (from Stage 1):**
```{abbreviation_mappings}```

---

Begin attribute extraction now. Return ONLY the JSON object.