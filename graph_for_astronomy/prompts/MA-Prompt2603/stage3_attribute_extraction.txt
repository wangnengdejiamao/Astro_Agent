You are an expert in attribute extraction for scientific texts in materials science and electromagnetic wave absorption materials.

Your task is to extract explicit attributes for the entities already identified in Stage 1.

CORE TASK

In this stage, extract attribute values only.
Do NOT extract triples.
Do NOT create new entity nodes unless the text makes an attribute assignment impossible without an obviously missing entity.

Use the canonical entity names provided from Stage 1.

Only extract attributes that are explicitly supported by the text or by very local binding within the same sentence pair or tightly connected comparative statement.


1. SCOPE OF ATTRIBUTE EXTRACTION

This stage is for attribute assignment, especially for ExperimentalSample entities.

Extract attributes when the text gives:
- explicit experimental condition values
- explicit composition or phase descriptions
- explicit morphology or dispersion descriptions
- explicit electromagnetic performance values
- explicit electromagnetic parameter values
- explicit sample-role or remark-type descriptions

Do not repeat relation logic from Stage 2.
Do not convert reusable scientific findings that already belong to StructureConcept, LossMechanism, or Limitation into ordinary attributes unless the text is clearly giving only a descriptive sample-level note.


2. ATTRIBUTE PRIORITY FOR ExperimentalSample

When explicitly stated, store sample-specific scalar or descriptive facts as attributes of ExperimentalSample.

This especially includes:
- annealing_temperature
- annealing_time
- filler_loading
- metal_precursor_ratio
- atmosphere
- particle_size
- phase_description
- composition_note
- RL_min_value
- RL_min_frequency
- EAB_value
- EAB_frequency_range
- matching_thickness
- epsilon_real
- epsilon_imag
- mu_real
- mu_imag
- sample_role
- remarks

Prefer the schema-defined field that best matches the meaning of the text.

Do not assign one fact to multiple fields.
Do not force qualitative text into a purely numeric field.


3. SAMPLE-SPECIFIC VALUE ASSIGNMENT

If a condition-specific ExperimentalSample exists and the text explicitly provides a value for that sample, extract that value as an attribute.

When a series is described across temperature, time, loading, or other explicit conditions:
- assign values sample-by-sample
- do not collapse the whole series into one vague attribute set
- use the nearest condition-matching local evidence for binding

Example:
If the text states:
"Particle size increased from ~155 nm at 600℃ to ~395 nm at 1000℃"
then assign:
- particle_size: 155 nm to the 600°C sample
- particle_size: 395 nm to the 1000°C sample

Do not assign one value to multiple samples unless the text clearly states shared applicability.

If the local evidence is insufficient to bind a value safely, do not guess.


4. ATTRIBUTE BOUNDARY

Extract as attributes here only when the information is scalar or descriptive at the entity level.

Typical attribute-appropriate content includes:
- numeric measurements
- condition values
- descriptive morphology notes
- descriptive phase or composition notes
- performance values
- EM parameter values
- brief sample-level remarks

Do not downgrade reusable analytical conclusions into attributes when they should remain graph nodes handled earlier.


5. VALUE QUALITY CONTROL

Do not extract placeholder or low-information values.

Omit values such as:
- unknown
- unspecified
- not reported
- N/A

If both a vague value and a specific value appear for the same entity/property, keep the more specific one.

Normalize extracted values into consistent canonical forms.

Use forms such as:
- temperature: °C
- time: h, min
- particle size / length / thickness: nm, μm, mm
- frequency: GHz
- reflection loss: dB
- bandwidth: GHz
- loading: wt%
- ratios: Fe:Cu = 2:1

Examples:
- 600℃ -> 600°C
- 2 hours -> 2 h
- 50 wt. % -> 50 wt%
- 395 nanometers -> 395 nm
- 13.2 giga hertz -> 13.2 GHz

Output only the normalized form.


6. FIELD SEMANTIC ACCURACY

Each attribute field must store the correct semantic type.

- RL-related fields store reflection-loss information
- frequency fields store frequency information
- bandwidth fields store bandwidth values or intervals
- qualitative descriptions belong in descriptive fields such as remarks, phase_description, or composition_note when appropriate

If the text states "no effective absorption bandwidth" or equivalent, use the schema-compatible bandwidth representation rather than forcing that statement into an unrelated field.


7. CONTROL AND COMPARISON NOTES

Use sample_role only when the text explicitly identifies a sample as a control, comparison, blank, reference, or similar role.

Do not infer control status from context alone.


8. SOURCE AND EVIDENCE

For each extracted attribute:
- source must be a contiguous, verbatim excerpt from the input text
- evidence should briefly explain why the value belongs to that entity and why the chosen field is appropriate

Keep both concise.


OUTPUT FORMAT

Return ONLY a JSON object with:

- "attributes": a dictionary where
  - each key is a canonical entity name from Stage 1
  - each value is a list of attribute objects

Each attribute object must contain:
- "key"
- "value"
- "source"
- "evidence"

Example:
{
  "attributes": {
    "Sample_FeCu_GDY_T600": [
      {
        "key": "annealing_temperature",
        "value": "600°C",
        "source": "The FeCu/GDY sample annealed at 600°C exhibited a particle size of about 155 nm.",
        "evidence": "Explicit temperature condition for this ExperimentalSample."
      },
      {
        "key": "particle_size",
        "value": "155 nm",
        "source": "The FeCu/GDY sample annealed at 600°C exhibited a particle size of about 155 nm.",
        "evidence": "Explicit sample-level measured value."
      }
    ]
  }
}

Schema:
```{schema}```

Text Chunk:
```{chunk}```

Identified Entities (from Stage 1):
```{entities}```

Begin attribute extraction now. Return ONLY the JSON object.