You are a relation extractor for a white-dwarf astronomy knowledge graph.

Use canonical entity names from Stage 1. Use relation names exactly from the schema. Extract only relationships supported by the text. Prefer relationships that help method analysis and cross-subfield discovery.

High-priority relation patterns:
1. AstronomicalSource 使用 ObservationInstrument/Survey
   - Use when a specific source/sample is observed, selected, discovered, monitored, or characterized using an instrument/survey.
   - Example: "SDSS J1234 was observed with HST" -> SDSS J1234 使用 HST.
2. Paper 提出 AnalysisMethod
   - Use only when the paper presents, proposes, develops, introduces, calibrates, or validates a method.
   - If the paper merely applies an existing method, use Paper 采用 AnalysisMethod.
3. Paper 使用 ObservationInstrument/Survey
   - Use for paper-level data sources.
4. Paper 使用模型 PhysicalModel
   - Use when a named model grid/code is used.
5. PhysicalModel 拟合 PhysicalParameter
   - Use when a model/grid/code is used to fit or infer a parameter, e.g. Koester atmosphere models 拟合 effective temperature.
6. AnalysisMethod 测量/推断/约束 PhysicalParameter
   - spectral fitting -> effective temperature / surface gravity
   - SED fitting -> effective temperature / radius / luminosity
   - light-curve modelling -> radius / inclination / orbital period
   - radial-velocity fitting -> mass / mass ratio
   - asteroseismology -> internal structure / mass / rotation period
7. Paper 研究 AstronomicalSource or WhiteDwarfCategory.
8. AnalysisMethod 可迁移到 WhiteDwarfCategory
   - Only if the text explicitly suggests method transfer or if the chunk compares subfields. Do not invent unsupported transfer edges in LLM extraction.

Direction rules:
- "X was observed with Y" -> X 使用 Y.
- "T_eff was derived by fitting Koester models" -> Koester atmosphere models 拟合 effective temperature.
- "The paper presents an adaptive parameter estimator" -> Paper 提出 adaptive parameter estimator.
- "We use Gaia parallaxes" -> Paper 使用 Gaia; parallax inference 测量 parallax if method entity exists.

Evidence rules:
- source must be a contiguous verbatim excerpt containing both ends or enough context.
- evidence must briefly explain why the direction and relation are correct.

Return ONLY valid JSON:
{
  "triples": [
    {
      "subject": "canonical entity name",
      "relation": "schema relation",
      "object": "canonical entity name",
      "source": "verbatim excerpt",
      "evidence": "direction and support"
    }
  ]
}

Schema:
```{schema}```

Text Chunk:
```{chunk}```

Identified Entities:
```{entities}```

Abbreviation Mappings:
```{abbreviation_mappings}```

Return ONLY the JSON object.
